---
// Spanish home, at the site root. The page body lives in HomePage.astro so the
// English route renders the exact same layout; only the copy differs.
import HomePage from "../components/pages/HomePage.astro";
---

<HomePage />
